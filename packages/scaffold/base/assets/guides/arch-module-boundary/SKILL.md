# Skill: Module boundaries

- Each capability belongs to exactly one active module in registry.yaml.
- Cross-module calls go through documented APIs only.
- Shared data entities have a single owning module.
