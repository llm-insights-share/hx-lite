# Skill: Global architecture (HLD)

When writing `docs/architecture/overview.md`:

1. Define system boundaries and upstream/downstream systems.
2. Module table must list responsibility, inputs, outputs, constraints.
3. Record ADR with Decision, Alternatives, Consequences.
4. Keep `registry.yaml` in sync with module table.
5. Map each module to one or more capabilities (spec domains).
