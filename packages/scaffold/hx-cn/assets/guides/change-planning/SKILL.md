# 开发计划

产出双轨 `tasks.md` 与设计交接元数据。

## 何时使用
- 任务 `dev.plan`

## 覆盖
- `[test]`/`[impl]` 覆盖 delta Scenario；`@design=` / `@files=`；必要时用 `rollback-template`

## 规则
- 勿删除 `[test]`；Scenario 名须与 delta 一致

## 完成条件
plan 门禁绿灯
