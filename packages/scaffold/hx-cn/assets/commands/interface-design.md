# /hx-arch-interface-design — 接口设计

你正在执行 **arch** 阶段任务 `interface-design`。

## Input
- 已有 HLD overview。

## Steps
1. 填写外部/系统间接口章节。
2. 遵循 **arch-authoring**。
3. `hx arch check --task interface-design`。

## Output
- 有实质内容的接口设计章节。

## Guardrails
- 仅组织级；不在此写 change 级 OpenAPI。

## Done when
`hx arch check --task interface-design` 绿灯。
